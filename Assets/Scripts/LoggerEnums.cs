using UnityEngine;

public enum LoggerType
{
    None = 0,
    LogOnly,
    PrintOnly,
    LogAndPrint
}

public enum LoggerScreenPosition
{
    TopLeft,
    TopRight,
    // BottomLeft,
    // BottomRight,
}